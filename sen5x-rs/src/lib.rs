#![no_std]

pub struct Sen5xDriver;

impl Sen5xDriver {
    /// Initializer mimicking the device handle lifecycle
    pub fn new() -> Self {
        Self
    }
}

impl Default for Sen5xDriver {
    fn default() -> Self {
        Self
    }
}